#include <stdio.h>

int main()
{

    // int n = 5;
    // int *ptr = &n;

    // printf("%u\n", ptr);
    // printf("%u\n", ptr + 1);
    // if we add one the pointer address is increment to bit of the operator ex. int - 4bit

    // int n = 5;
    // int *ptr = &n;

    // printf("%u\n", ptr);
    // printf("%u\n", ptr - 1);
    // if we substract one to the pointer address is decrement to bit of the operator ex. int - 4bit

    // int arr[] = {10, 20, 30, 40, 50};
    // int *ptr1 = &arr[1];
    // int *ptr2 = &arr[4];

    // int difference = ptr2 - ptr1;
    // printf("Difference between pointers: %d\n", difference);

    // int arr[] = {10, 20, 30, 10};
    // int *ptr1 = &arr[0];
    // int *ptr2 = &arr[3];
    // int com = ptr1 == ptr2;
    // printf("Comparison of two pointers is %d\n", com);
    
    return 0;

    return 0;
}
